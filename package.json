{
  "name": "cdk-lambda-child",
  "version": "1.0.0",
  "description": "export async function handler(event: string, context: string) { console.log(\"Stage Name is: \" + process.env.stage); return { body: \"Hello from a Lambda Function\", statusCode: 200, }; }",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.12.11"
  }
}
